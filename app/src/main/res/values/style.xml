<?xml version="1.0" encoding="utf-8"?>
<resources>


    <style name="OrangeDatePickerDialogTheme" parent="ThemeOverlay.MaterialComponents.MaterialCalendar">
        <!-- Основной цвет -->
        <item name="colorPrimary">@color/orange_500</item>
        <item name="colorPrimaryVariant">@color/orange_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Цвет фона -->
        <item name="android:windowBackground">@color/white</item>
        <!-- Цвет текста -->
        <item name="android:textColorPrimary">@color/orange_900</item>
        <item name="android:textColorSecondary">@color/orange_700</item>
        <!-- Цвет акцента -->
        <item name="colorAccent">@color/orange_500</item>
        <!-- Цвет кнопок -->
        <item name="buttonBarPositiveButtonStyle">@style/OrangeDatePickerDialogButtonStyle</item>
        <item name="buttonBarNegativeButtonStyle">@style/OrangeDatePickerDialogButtonStyle</item>
    </style>

    <!-- Стиль для кнопок -->
    <style name="OrangeDatePickerDialogButtonStyle" parent="Widget.MaterialComponents.Button.TextButton.Dialog">
        <item name="android:textColor">@color/orange_500</item>
    </style>

    <style name="TabTextStyle" parent="@android:style/TextAppearance.Widget.TabWidget">
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:maxLines">1</item>
        <item name="android:ellipsize">end</item>
    </style>
</resources>